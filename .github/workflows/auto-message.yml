name: Auto Message on Push

on:
  push:
    branches: [ "main" ]

jobs:
  print-message:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Print Message
        run: python message.py
